# History:
# 2025-04-09: Cline - Initial cleanup. Reduced the list to core Flask app dependencies.
#                     Original file contained many unrelated packages and local file paths,
#                     likely from a specific Conda environment export.
#                     Keeping only Flask and Werkzeug for clarity and reproducibility.
# 2025: Pinned exact versions for reproducible, stable builds on Python 3.10
#       (Flask 3.0.x + Werkzeug 3.0.x are the final 3.0 releases, both require
#       Python >=3.8; gunicorn 22.0.0 includes the CVE-2024-1135 fix).
# 2025: Security bumps - Werkzeug 3.0.6 fixes CVE-2024-49766/49767 (debugger);
#       gunicorn 23.0.0 fixes CVE-2024-6827 (request smuggling).

Flask==3.0.3
Werkzeug==3.0.6
gunicorn==23.0.0
geoip2==4.7.0
